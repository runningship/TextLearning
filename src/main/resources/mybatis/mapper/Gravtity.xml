<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Gravity">

	<select id="listGravity" parameterType="paramMap" resultType="Map">
		select * from gravity 
		<!-- where next <![CDATA[ <> ]]> ''  -->
	</select>
	
	
	<select id="clear" parameterType="paramMap" resultType="Map">
		delete from gravity
	</select>
</mapper>